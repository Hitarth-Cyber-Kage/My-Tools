# Installation Guide for Ninja Lock

## Step 1: Install Python
Ensure that you have Python 3.x installed on your system. You can download it from the official website: https://www.python.org/downloads/

## Step 2: Install termcolor Library
Open your terminal or command prompt and run the following command to install the `termcolor` library:

## Step 3: Download Ninja Lock
1. Download the `ninja_lock.py` file from the project repository or location where it is shared.
2. Save the file in a directory of your choice.

## Step 4: Run the Tool
1. Open your terminal or command prompt.
2. Navigate to the directory where `ninja_lock.py` is located using the `cd` command. For example:
3. Run the program with the following command:

## Step 5: Follow On-Screen Instructions
Once the program is running, follow the on-screen instructions to generate passwords and PINs.

## Troubleshooting
- If you encounter an error stating that `termcolor` is not found, ensure that it is installed correctly by running: